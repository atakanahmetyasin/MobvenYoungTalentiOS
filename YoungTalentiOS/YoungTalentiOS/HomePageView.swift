import SwiftUI

struct HomePageView: View {
    let mainUrl = Bundle.main.infoDictionary?["BACKEND_URL"] as? String
    
    var body: some View {
        VStack {
           
        }
        .padding()
    }
}

#Preview {
    HomePageView()
}
